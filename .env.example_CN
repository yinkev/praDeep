# ============================================================================
# Deep-Tutor 环境配置文件
# ============================================================================
# 将此文件复制为 .env 并填写您的 API 密钥和设置。
# 所有 API 密钥应妥善保管，切勿提交到版本控制系统中。
# 您可以在 praDeep/config/agents.yaml 中调整 max_token 参数。
# ============================================================================

# ============================================================================
# 服务器配置
# ============================================================================
# 配置服务器端口和远程访问的 API URL。

# 后端 API 端口（默认：8001）
# BACKEND_PORT=8001

# 前端 Web 端口（默认：3782）
# FRONTEND_PORT=3782

# 前端 API 基础 URL（用于远程/局域网访问）
# 当从网络中的其他设备访问 praDeep 时设置此项。
# 示例：如果您的服务器 IP 是 192.168.1.100，请设置：
#   NEXT_PUBLIC_API_BASE=http://192.168.1.100:8001
# 如果未设置，默认为 http://localhost:8001（仅在本机有效）

# NEXT_PUBLIC_API_BASE=http://your-server-ip:8001（可选）

# ============================================================================
# LLM 部署模式配置
# ============================================================================
# 控制 praDeep 如何选择 LLM 提供商。
#
# 选项：
#   - hybrid（默认）：同时使用 API 和本地提供商。活动的提供商优先。
#   - api：仅使用云端 API 提供商（OpenAI、Anthropic、DeepSeek 等）
#   - local：仅使用本地/自托管提供商（Ollama、LM Studio、vLLM 等）
#
# 在 hybrid 模式下，您可以通过设置界面添加 API 和本地提供商，
# 并通过将其设为"活动"来在它们之间切换。

LLM_MODE=hybrid

# ============================================================================
# LLM（大语言模型）配置
# ============================================================================
# 配置用于推理、生成和对话的主要 AI 模型。
# 支持 OpenAI 兼容 API（OpenAI、Azure OpenAI、DeepSeek、Qwen 等）
# 以及本地服务器（Ollama、LM Studio、vLLM、llama.cpp）。
#
# 注意：这些环境变量作为默认/后备配置。
# 您也可以通过设置界面添加提供商，活动时将优先使用。

# LLM 服务提供商类型
# 选项：openai、azure_openai、ollama、anthropic
LLM_BINDING=openai

# LLM 模型名称
# 云端示例：gpt-4o、gpt-4o-mini、deepseek-chat、claude-3-5-sonnet-20241022
# 本地示例：llama3.2、qwen2.5、mistral-nemo、deepseek-r1
LLM_MODEL=

# LLM API 端点 URL
# 云端示例：
#   - OpenAI：https://api.openai.com/v1
#   - DeepSeek：https://api.deepseek.com
#   - Anthropic：https://api.anthropic.com/v1
# 本地示例：
#   - Ollama：http://localhost:11434/v1
#   - LM Studio：http://localhost:1234/v1
#   - vLLM：http://localhost:8000/v1
#   - llama.cpp：http://localhost:8080/v1
LLM_HOST=

# LLM API 认证密钥
# 云端 API 必需，本地部署可选
# 对于 Ollama，可以使用任意字符串（如 "ollama"）
LLM_API_KEY=

# 禁用 SSL 证书验证（自签名证书时设置为 'true'）
DISABLE_SSL_VERIFY=false

# ============================================================================
# 嵌入模型配置
# ============================================================================
# 配置用于语义搜索和 RAG 的文本嵌入模型。
# 知识库功能必需。

# 嵌入服务提供商类型
# 选项：openai、azure_openai、ollama、lollms
EMBEDDING_BINDING=openai

# 嵌入模型名称
# 云端示例：text-embedding-3-large、text-embedding-3-small、text-embedding-ada-002
# 本地示例（Ollama）：nomic-embed-text、mxbai-embed-large
EMBEDDING_MODEL=text-embedding-3-large

# 嵌入向量维度
# text-embedding-3-large：3072
# text-embedding-3-small：1536
# nomic-embed-text：768
# mxbai-embed-large：1024
EMBEDDING_DIMENSION=3072

# 嵌入 API 端点 URL
# 云端：https://api.openai.com/v1
# 本地（Ollama）：http://localhost:11434
EMBEDDING_HOST=

# 嵌入 API 认证密钥
# 云端提供商必需，Ollama 不需要
EMBEDDING_API_KEY=

# ============================================================================
# TTS（文本转语音）配置（可选）
# ============================================================================
# 配置 Co-Writer（交互式创意生成器）朗读功能的语音合成。
# 同时记得在 praDeep/config/main.yaml 中选择语音。

# TTS 模型名称（如 tts-1、tts-1-hd）
TTS_MODEL=

# TTS API 端点 URL（如 https://api.openai.com/v1）
TTS_URL=

# TTS API 认证密钥
TTS_API_KEY=

# ============================================================================
# 网络搜索配置
# ============================================================================
# 配置用于研究功能的外部搜索 API。

# 用于网络搜索的搜索提供商
# 选项：perplexity、baidu
# 默认：perplexity
SEARCH_PROVIDER=perplexity

# ----------------------------------------------------------------------------
# Perplexity AI 搜索配置
# ----------------------------------------------------------------------------
# Perplexity API 密钥，用于网络搜索功能
# 在此获取 API 密钥：https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# ----------------------------------------------------------------------------
# 百度 AI 搜索配置
# ----------------------------------------------------------------------------
# 百度 API 密钥，用于智能搜索和生成
# 在此获取 API 密钥：https://console.bce.baidu.com/ai_apaas/resource
# 格式：bce-v3/ALTAK-xxx/xxx 或完整的 API 密钥
BAIDU_API_KEY=

# ============================================================================
# 日志配置
# ============================================================================

# RAG 工具模块的日志级别
# 选项：DEBUG、INFO、WARNING、ERROR
RAG_TOOL_MODULE_LOG_LEVEL=INFO
